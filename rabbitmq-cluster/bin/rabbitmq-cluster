require 'rabbitmq/cluster'

server = RabbitMQ::Cluster::Server.build

until server.up?
  while true do
    begin
      server.synchronize
    rescue => e
      puts e.message
      puts e.backtrace
    end
    sleep 5
  end
  sleep 1
end
